## RESTServer en Node.js y MongoDB

De momento solo hace 4 peticiones basicas (POST/GET/PUT/DELETE) y devuelve valores por pantalla

No olvide instalar las dependencias de Node.js con el comando
```npm install```